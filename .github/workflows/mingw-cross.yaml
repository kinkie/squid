# development workflow; to be eventually moved to quick.yaml
# Test a mingw cross-compile build on a Linux host

name: MinGW

on:
  push:
    branches: [ 'github-action-*' ]

concurrency:
  # Cancel ongoing tests in case of push to the same PR or staging branch,
  # but let previous master commit tests complete.
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:

  mingw-cross:
    runs-on: squidcache/buildfarm-ubuntu-noble:stable
    env:
      BUILDCXX: g++
      # BUILDCXXFLAGS: -DFOO

    steps:
      - name: Install prerequisite packages
        run: |
          sudo apt-get --quiet=2 update
          sudo apt-get --quiet=2 install mingw-w64 wine-binfmt

      - name: Checkout Squid sources
        uses: actions/checkout@v4

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2.14
        with:
          verbose: 2 # default 0
          key: mingw-cross

      - name: Run build on MinGW
        id: test-builds
        # we cannot rely on test-builds.sh because it doesn't
        #  support overrinding test layer configure arguemnts
        run: |
          ./bootstrap.sh
          ./configure --host=x86_64-w64-mingw32 --disable-ssl --without-openssl
          make -j`nproc` all check
        #  ./test-suite/test-functionality.sh

      - name: Publish build logs
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-mingw
          path: btlayer-*.log