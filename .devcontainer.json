{
    "name": "Squid MinGW"
    , "image": "squidcache/buildfarm-fedora-mingw:latest"
    , "remoteUser": "${localEnv:USER}"
    , "containerUser": "${localEnv:USER}"
    , "updateRemoteUserUID": false
    , "runArgs": [ "--platform", "linux/amd64" ]
    , "mounts": [
        {
            "source": "mingw-kinkie",
            "target": "/home/${localEnv:USER}",
            "type": "volume"
        },
        {
            "source": "${localEnv:HOME}/src/squid",
            "target": "/home/${localEnv:USER}/src/squid",
            "type": "bind"
        },
        {
            "source": "${localEnv:HOME}/.gitconfig",
            "target": "/home/${localEnv:USER}/.gitconfig",
            "type": "bind",
            "readonly": true
        },
        {
            "source": "${localEnv:HOME}/.gitignore",
            "target": "/home/${localEnv:USER}/.gitignore",
            "type": "bind",
            "readonly": true
        },
        {
            "source": "${localEnv:HOME}/bin",
            "target": "/home/${localEnv:USER}/bin",
            "type": "bind",
            "readonly": true
        },
        {
            "source": "${localWorkspaceFolder}",
            "target": "/workspaces/${localWorkspaceFolderBasename}",
            "type": "bind"
        }
    ]
    , "workspaceMount": "type=bind,source=${localWorkspaceFolder},target=${localWorkspaceFolder}"
    , "workspaceFolder": "${localWorkspaceFolder}"
    }
}
